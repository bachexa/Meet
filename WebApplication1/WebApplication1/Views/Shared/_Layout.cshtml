@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MeetDesk - Meet Persons online, Chat, MakeConference </title>

    <!-- Default styles -->
    @await Html.PartialAsync("_DefaultStyles")

    <!-- Additional page-specific styles -->
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    <header class="ms-header">
    <div class="ms-container">
        <!-- Left: Logo + Brand -->
        <div class="ms-left">
            <a href="/" style="text-decoration: none;">
    <span style="font-family: 'Segoe UI', sans-serif; font-size: 20px; font-weight: 600; color: #1a1a1a;">
        <span style="color: #0078d4;">Meet</span>Desk
    </span>
    </a>
        <div class="ms-divider"></div>
            
        </div>

        <!-- Mobile toggle button -->
        <button class="ms-toggle" onclick="toggleMenu()">☰</button>

        <!-- Center: Nav -->
        <nav class="ms-nav" id="mainMenu">
            <a href="#">Products</a>
            <a href="#">Features</a>
            <a href="#">Pricing</a>
            <a href="#">Solutions</a>
            <a href="#">Resources</a>
            <a href="#">Support</a>
        </nav>

        <!-- Right: Buttons -->
        <div class="ms-buttons">
            <button class="ms-btn" type="button">Download MetDesk</button>
            <button class="ms-btn signin-btn" type="button">Sign in</button>
        </div>
    </div>
</header>


<section class="hero-section">
  <div class="hero-content">
    <h1>Get ready for the future<br>of work with <span>Microsoft Teams</span></h1>
    <p>Transform the way you work with next-generation AI capabilities and bring together your physical and digital worlds.</p>
    <a href="#" class="download-btn">Download now</a>
  </div>
  <div class="hero-image">
    <img src="/images/teams-hero.png" alt="Meet Desk Image">
  </div>
</section>


    @* <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">MeetDesk</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Product" asp-action="Index">Product</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header> *@

    <div class="container my-4">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="footer mt-auto py-3 bg-light">
        <div class="container">
            <span class="text-muted">&copy; 2025 - MyApp - <a asp-controller="Home" asp-action="Privacy">Privacy</a></span>
        </div>
    </footer>

    <!-- Default scripts -->
    @await Html.PartialAsync("_DefaultScripts")

    <!-- Additional page-specific scripts -->
    @await RenderSectionAsync("Scripts", required: false)

    

</body>
</html>
